{
  "name": "@agentprism/driver-openai-agents",
  "description": "OpenAI Agents SDK driver: agents, guardrails and tool calls over the Arena model port.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentprism/contracts": "workspace:*",
    "@agentprism/driver-run-support": "workspace:*",
    "@agentprism/harness": "workspace:*",
    "@openai/agents": "^0.18.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@agentprism/runtime": "workspace:*",
    "@agentprism/telemetry": "workspace:*",
    "@agentprism/tool-builtins": "workspace:*",
    "@types/node": "^24"
  }
}
