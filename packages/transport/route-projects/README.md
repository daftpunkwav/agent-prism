# `@agentprism/route-projects`

Project domain routes: `registerProjectRoutes(app, deps)` mounts project listing, archive-from-run, and deletion endpoints.

## Dependencies

- Runtime: `application / contracts / http-runtime`.
