/**
 * @file catalogs/zh-CN/learn
 * @description zh-CN copy for the /learn view (source of truth).
 *
 * Responsibilities:
 * - Define the learn namespace's keys and Chinese copy
 *
 * Pure data only; the en overlay must stay structurally identical.
 */

export const learn = {
  cta: {
    step: "开始这一步",
    stepAria: "开始第 {week} 周：{title}",
  },
};
