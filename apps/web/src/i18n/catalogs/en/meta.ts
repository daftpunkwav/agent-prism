/**
 * @file catalogs/en/meta
 * @description English copy for document metadata.
 *
 * Responsibilities:
 * - Mirror the zh-CN meta namespace key-for-key
 *
 * Structure is compile-enforced via MessageCatalog against the zh-CN source.
 */

export const meta = {
  title: "AgentPrism — Agent Comparison Arena",
  description:
    "Run the same question through controlled parallel pipelines — differences you can quantify and reproduce",
};
