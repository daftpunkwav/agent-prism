{
  "name": "@agentprism/driver-crewai",
  "description": "CrewAI driver: real crewai crew over a Python bridge when available, TypeScript pattern fallback otherwise.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "python"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentprism/contracts": "workspace:*",
    "@agentprism/driver-run-support": "workspace:*",
    "@agentprism/harness": "workspace:*",
    "@agentprism/telemetry": "workspace:*"
  },
  "devDependencies": {
    "@agentprism/runtime": "workspace:*",
    "@agentprism/tool-registry": "workspace:*",
    "@types/node": "^24"
  }
}
