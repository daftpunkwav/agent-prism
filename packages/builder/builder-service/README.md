# `@agentprism/builder-service`

Builder service: session lifecycle, catalog, hot-swaps. Orchestrates turn execution from `builder-turns`; the service's own heavy dependency is tool assembly (`tool-builtins`).

## Dependencies

- Runtime: `contracts / persistence / runtime / tool-builtins / builder-turns`.
