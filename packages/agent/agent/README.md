# `@agentprism/agent`

Single-column execution lifecycle: runs one column by composing the harness with the tool seam. Framework-agnostic.

## Dependencies

- Runtime: `contracts / runtime / telemetry / harness / sandbox / tool-registry / tool-builtins`.
