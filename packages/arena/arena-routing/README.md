# `@agentprism/arena-routing`

Dimension routing and baselines: pure config semantics from dimensions to pipeline configs, never touching execution.

## Dependencies

- Runtime: `contracts / dimensions / harness`. No `agent`, no LangChain.
