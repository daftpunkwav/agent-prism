# `@agentprism/route-sessions`

> 语言：**简体中文** | [English](README.md)

Session 路由：`registerSessionRoutes` 挂载列表，支持 `kind`、`status`、`limit` 过滤，
以及详情与删除 endpoint。删除 endpoint 用于账本达到上限前的运维清理。

## 依赖

- Runtime：`application / contracts / http-runtime`。
