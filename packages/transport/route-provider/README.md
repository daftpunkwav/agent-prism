# `@agentprism/route-provider`

Provider-config domain routes: `registerProviderRoutes(app, deps)` mounts config read/write and connectivity-test endpoints.

## Dependencies

- Runtime: `application / http-runtime`.
