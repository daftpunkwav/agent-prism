# `@agentprism/route-workspace`

Workspace-file domain routes: `registerWorkspaceRoutes(app, deps)` mounts file list and read/write endpoints.

## Dependencies

- Runtime: `application / contracts / http-runtime`.
