# `@agentprism/route-provider`

> 语言：**简体中文** | [English](README.md)

Provider config 领域路由：`registerProviderRoutes(app, deps)` 挂载 config 读写与连通性
测试 endpoint。

## 依赖

- Runtime：`application / http-runtime`。
