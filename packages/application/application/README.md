# `@agentprism/application`

Use-case services: the four arena / provider / workspace / projects services plus domain errors.

## Dependencies

- Runtime: `arena-routing / arena-runner / contracts / environment / persistence / runtime`.
