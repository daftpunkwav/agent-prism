# `@agentprism/context-analytics`

Context usage measurement and strategy effectiveness counters.

- `usage`: per-turn token accounting across context sources.
- `effectiveness`: keep/drop/ledger counters per strategy for ablation grounding.
- `report`: deterministic text summaries for logs and narratives.
- Zero dependencies (pure data structures, deterministic).
