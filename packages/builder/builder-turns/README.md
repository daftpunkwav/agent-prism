# `@agentprism/builder-turns`

Turn execution: turn driving through `agent`, composition-to-pipeline config, builder blocks, trace log, and the `BuilderError` domain error. The service leaf consumes this leaf; the direction never reverses.

## Dependencies

- Runtime: `contracts / runtime / agent / arena-view`.
