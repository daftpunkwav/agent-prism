# `@agentprism/arena-routing`

> 语言：**简体中文** | [English](README.md)

Dimension 路由与 baselines：从 dimensions 到 pipeline config 的纯配置语义，绝不接触
执行。

## 依赖

- Runtime：`contracts / dimensions / harness`。不依赖 `agent`，不依赖 LangChain。
