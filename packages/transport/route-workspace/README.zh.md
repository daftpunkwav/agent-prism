# `@agentprism/route-workspace`

> 语言：**简体中文** | [English](README.md)

Workspace 文件领域路由：`registerWorkspaceRoutes(app, deps)` 挂载文件列表与读写
endpoint。

## 依赖

- Runtime：`application / contracts / http-runtime`。
