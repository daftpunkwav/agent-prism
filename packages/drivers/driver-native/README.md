# `@agentprism/driver-native`

In-process native driver (`NativeDriver`, `frameworkId "native"`): turn flow and tool batching, no external framework dependency.

## Dependencies

- Runtime: `contracts / driver-run-support / harness / telemetry`. LangChain-free.
