# `@agentprism/arena-runner`

Parallel multi-column execution: `ArenaRunner` (breaker/semaphore/event-channel orchestration); the column-factory port lives in contracts as `ColumnRuntimeFactory`.

## Dependencies

- Runtime: `contracts / runtime / agent`, plus `arena-dimensions` types.
