/**
 * @file catalogs/en/learn
 * @description English copy for the /learn view.
 *
 * Responsibilities:
 * - Mirror the zh-CN learn namespace key-for-key
 *
 * Structure is compile-enforced via MessageCatalog against the zh-CN source.
 */

export const learn = {
  cta: {
    step: "Start this step",
    stepAria: "Start week {week}: {title}",
  },
};
