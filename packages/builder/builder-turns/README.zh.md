# `@agentprism/builder-turns`

> 语言：**简体中文** | [English](README.md)

Turn 执行：经 `agent` 驱动 turn、composition 到 pipeline config、builder blocks、
trace log，以及 `BuilderError` 领域错误。service leaf 消费本 leaf，方向绝不反转。

## 依赖

- Runtime：`contracts / runtime / agent / arena-view`。
