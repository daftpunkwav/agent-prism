# `@agentprism/route-sessions`

Session routes: `registerSessionRoutes` mounts list (`kind`/`status`/`limit` filters), detail, and deletion endpoints (deletion exists for ops cleanup before the ledger hits its caps).

## Dependencies

- Runtime: `application / contracts / http-runtime`.
