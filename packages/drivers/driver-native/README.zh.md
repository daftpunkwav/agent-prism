# `@agentprism/driver-native`

> 语言：**简体中文** | [English](README.md)

进程内 native driver，`NativeDriver`，`frameworkId "native"`：turn 流程与 tool 批处理，
无外部 framework 依赖。

## 依赖

- Runtime：`contracts / driver-run-support / harness / telemetry`。不使用 LangChain。
