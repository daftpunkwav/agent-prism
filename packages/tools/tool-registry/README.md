# `@agentprism/tool-registry`

Tool seam: the in-memory `MapToolRegistry` plus toolset resolution. Zero tool implementations and zero `environment` dependency, which makes it the lowest-level tool-contract holder.

## Dependencies

- Runtime: `contracts` only.
